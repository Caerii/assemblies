# Volume 11: Scaling, Engines, And Performance

This volume covers the engineering substrate: engines, sparse computation,
winner selection, backend heuristics, and accelerator plans.

## Notebooks

- `01_engine_and_policy_map.ipynb`: inspect backend selection and winner-policy
  objects without running hardware-sensitive benchmarks.

## Future Notebooks

- CPU sparse versus dense break-even checks
- GPU backend smoke tests when hardware is present
- top-k and threshold-policy comparisons
- memory estimates for large `N`
- quantization and precision experiments
- performance result-artifact walkthroughs

Treat backend heuristics as engineering defaults, not universal performance
claims.
