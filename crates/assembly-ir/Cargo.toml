[package]
name = "assembly-ir"
version = "0.1.0"
edition = "2021"
description = "Assembly Calculus IR v1 types and JSON validation"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
