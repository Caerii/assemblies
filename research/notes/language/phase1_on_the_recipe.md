# Phase 1 on the recipe — the law transfers attenuated, and the residual at ceiling is collision load

**Task #151 close-out unit (registered `fd8b1c4`, bars and
interpretation rules pre-stated). Brown at n=10⁵, full recipe, fixed
engine, seeds 42–51. Figures: research/figures/fig_151_*.**

## Verdicts

- **P1 HEADLINE REPLICATION — EXACT.** Every per-seed stratum matches
  adoption_gate_results.json to the digit: cross-script determinism
  holds (the one-seeding-path guarantee, verified rather than
  assumed).
- **P2 EXPOSURE LAW RE-PRICED — transfers, attenuated, as predicted.**
  ρ(exposure, correctness) over PL forms = **0.26 ± 0.05** (CI
  excludes 0), down from the pre-fix 0.61. Per the pre-registered
  interpretation rule this is attenuation-by-success (range
  restriction at ceiling), NOT transfer failure — and the refutation
  condition (significantly negative ρ) did not occur. ρ is undefined
  for exactly one seed — the ALL-CORRECT one (49); reported, not
  averaged away ([[undefinedness-correlates-with-outcome]]).
- **P3 THE RESIDUAL AT CEILING IS COLLISION LOAD.** All 41 failing
  probes (of 460) are E=1 words, and failing words carry **64% more
  SG-shared weighted core rows** than passing ones (10.3 ± 0.9 vs
  6.3 ± 0.3, disjoint CIs). The A3 "collisions are secondary"
  finding inverts at ceiling: once exposure is paid, collision load
  is what remains. This table feeds the open theory unit: why does
  E=1 read 0.85 when the naive Remark-2 arithmetic (boost 2.5 <
  extreme factor 3.3) says it cannot?
- **P4 MECHANISM LIVENESS — scaling is DORMANT in the recipe**
  (deferred=False, no scaled areas), so the E19 schedule-wall claims
  are formally scoped to the synthetic configuration and are not part
  of the production-recipe claim. Guard: SG = 1.000 on every seed.

## What this closes

#151's registered arc is complete: attribution → readout campaign →
literature → paper-regime 2×2 → scale cell → per-seed attribution →
adoption gate → dead-fiber hunt → fix → gate at ceiling → Phase 1 on
the recipe. The production recipe is documented in
production_configuration.md with the engine-version requirement
(≥ 53e9808) stated.

## The two open questions this creates

1. **E=1 beats the margin arithmetic** — the campaign's own closed
   form under-predicts the fixed engine. Candidate accounts to
   register: within-episode rounds compound differently once rows are
   covered; the E=1 words' single episodes now write full mass and the
   46-assembly area is lightly loaded. Registered bars must come
   before any measurement.
2. **Collision load as the terminal residual** — predicts the recipe's
   ceiling under vocabulary growth (V·k²/n rising), connecting back to
   the extensive-capacity law. The lever list is already priced: n up,
   or per-fiber p.

## Next

Propagate the recipe: role binding (#52) carries the same
label-attractor signature the 2×2 killed; then the ERP readout (#121).
