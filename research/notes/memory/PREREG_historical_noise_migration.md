# Historical noise protocol: software migration registration

This is a numerical migration, not registration of a new scientific adoption.
Preserve H1/H2/H3/H4 learning-on-recovery schedules and pre-association references
as described in docs/reviews/whole-codebase/SEMANTIC_CARDS.md. The default full grid
is n1000,k100,p.05,beta.1,clip20,30 establishment rounds after the initial stimulus
round,20 recovery rounds, noise 0,.1,.2,.3,.4,.5,.6,.8,1; H4 sizes200/500/1000/2000,
k=floor(sqrt(n)), noise .3/.5/.7/1. Default brain identities are explicitly 42..51.
Primary engine numpy_sparse, actual area owner numpy_explicit; recovery learns.

The runner must capture all parameters and actual seed identities before compute,
reserve an exclusive tagged directory, retain raw per-seed values for every cell,
and preserve source and this registration. UNADOPTED means no adoption criteria
were registered; significant historical t-tests are not automatically new results.
Do not infer missing seed/source provenance in older committed scientific artifacts.

Before adopting this software migration, all 27 captured pre-refactor trial fixtures
must retain schedules, full winner trajectories, final weight digests and outputs.
A reduced shared-runner check must match the same configured direct path's metrics
and raw observations, excluding elapsed time and wall-clock timestamps. Smoke uses
n60,k6,p.2,3 establishment/3 recovery rounds, noise0/.5/1, H4 size60 and noise0/.5/1.
At least three explicit seeds are required. Every smoke observation is VOID.
The original CLI must also require --tag; --quick is a VOID smoke alias.

This registration precedes the shared-runner smoke. It neither certifies historical
scientific claims nor supplies the separate frozen-recovery protocol's null sweep.
