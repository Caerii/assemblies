# The gate passed by its letter and caught a dead fiber — adoption holds on a one-in-ten silent PL death

**Task #151, adoption-gate unit (registered `eef5b7b`, bars and the
adoption form fixed before the run). Brown at n=10⁵ under the full
recipe, seeds 42–51; synthetic #149-gate protocol, seeds 42–46.**

## Verdicts

- **R2 PASSES cleanly — the synthetic guard is root-caused.** The paper
  regime's synthetic deficit concentrates at E=1 (Δ = −0.135 ± 0.098,
  CI excludes 0) while the E≥2 stratum is non-regressed (+0.014 ±
  0.179). The −0.07 that blocked adoption twice was the margin
  arithmetic at the synthetic corpus's tiny exposures, exactly as
  predicted — verified, not assumed.
- **R1 PASSES BY ITS LETTER and the required distribution shows why
  letters are not enough**: min(SG, PL) on the E≥2 exam = 0.778 ±
  0.227, mean over the bar — but per-seed: [1, .53, .63, **.05**, .95,
  .63, .995, 1, 1, 1]. Headline strata: SG E≥2 0.9995, PL E≥2 0.779 ±
  0.227, PL E=1 0.500 ± 0.223, balanced overall 0.807 ± 0.110.

## The forensic split (from data already in hand)

Per-seed own-drive across the 46 PL words separates THREE regimes:

| regime | seeds | signature |
|---|---|---|
| **DEFECT** | 45 | own = **0.0 EXACTLY on 45/46 words**; the one exception reads 0.679 |
| low margin | 43, 47 | own nonzero but ~0.027 (4× below healthy) |
| healthy | the other nine | median own ~0.11, PL 0.72–0.91 |

Exact zero on a trained fiber for every word but one is not
statistics — it is the dead-fiber signature
([[silent-no-op-dead-fibers]]): the NOUN_CORE→NUMBER_PL fiber is
effectively serving a single word's image, everything else reads
nothing, and the k-WTA still happily returns winners. This is the
content-addressed / deferred-init defect family
([[content-addressed-synapse-init]]: "ONE defect class with SIX
doors"; [[self-fibers-excluded-from-deferred-init]]), surfacing in a
sibling path — and task #50's multi-source deferred-init audit is
still open. The attribution unit's pooled statistics MASKED this
(pooling mixed one defect seed into an exposure story that is
otherwise correct); only the per-seed zero-count separates a defect
from a margin.

## Decision

Per the registered adoption form: defaults were never going to flip,
and the production-recipe documentation now ALSO holds — a recipe with
a ~1-in-10 chance of silently killing the rare class's fiber is not
adoptable. R2's pass stands (the synthetic guard question is closed
forever). ADOPTION BLOCKS ON THE DEFECT, which is the correct outcome:
the gate existed to catch exactly this.

## Registered next unit — the defect hunt

Reproduce the seed-45 cell (deterministic since the one-seeding-path
fix); census the NOUN_CORE→NUMBER_PL fiber against seed 42:
`materialized_count`, `fiber_extent`, `extent_desync`, and the weight
matrix's row/col coverage vs the 46 PL words' images; identify the one
served word and what made its columns different (first-materialized?).
Then walk the door checklist from
[[content-addressed-synapse-init]] — the prior five doors each looked
exactly like this. Low-margin seeds 43/47 are NOT the defect and wait
for the margin levers already priced (exposure, kp) — do not conflate
the two regimes.
